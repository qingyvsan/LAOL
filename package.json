{
  "name": "laol",
  "version": "0.1.0",
  "description": "Multi-agent collaborative coding system. Agents auto-discover target files, acquire atomic locks on-demand, and work in parallel with AI-powered semantic merge — zero databases, zero message queues.",
  "main": "dist/index.js",
  "bin": {
    "laol": "./bin/laol.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "commander": "^12.1.0",
    "uuid": "^9.0.1",
    "zod": "^3.23.8",
    "chalk": "^5.3.0",
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "@types/node": "^20.12.12",
    "@types/uuid": "^9.0.8"
  }
}
